server:
  port: 8081

spring:
  datasource:
    password: mausimj
    url: jdbc:postgresql://localhost:5432/DBForServiceLayer
    username: postgres
    platform: POSTGRESQL
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: none
